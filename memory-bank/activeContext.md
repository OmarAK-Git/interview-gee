# Active Context

## Current state

Sprint 5 (practice interviewer) is draining. `S5-T1` is done (verifier survives). Branch `practice-live-ui`.

## Immediate next step

Drain GSD autopilot from `S5-T2` (practice interviewer procedure in the skill). Live Luna demo stays a manual check on `S5-T7` / `S5-GE`; automated tests must not target real `~/.hermes`.

Carry into S5-T3: sanitize `pack_id` before path join in `require_session_jd` (code-reviewer minor).
