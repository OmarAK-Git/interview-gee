# Active Context

## Current state

Sprint 5 draining. `S5-T1` and `S5-T2` are done (verifier survives). Branch `practice-live-ui`.

## Immediate next step

Drain GSD autopilot from `S5-T3` (require one JD on session start). Carry: sanitize `pack_id` before path join in `require_session_jd`. Live Luna demo stays a manual check on `S5-T7` / `S5-GE`.
