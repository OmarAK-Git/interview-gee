# Workflow Plan — S1-T3

Researcher: **run** — live Hermes vs deterministic oracle vs contract+skip. Chosen: path 3 (SKILL + cases + bats; skip live N=5; fail-closed if live forced without binary).

Implementer required. Code review after. Verifier required. Test runner skipped (not a gate).
