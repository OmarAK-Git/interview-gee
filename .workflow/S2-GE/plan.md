# Workflow Plan — S2-GE

## Goal

Confirm the deliverable demo: memory-only opener works across separate processes with candidate exclusion (or recorded degraded path).

## Agent Plan

- Implementer: skipped — gates are verify-only.
- Test runner: required (`gates_and_major_sections`).
- Gate verdict: `cursor-grok-4.6-high-fast` in-session (Opus unavailable; gates use Grok).

## Work Packets

| ID | Objective | Ownership | Status |
| --- | --- | --- | --- |
| 01-gate | Phase-exit verification | test-runner + grok | in_progress |
